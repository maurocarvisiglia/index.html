name: Market Rumors daily cron

# Stesso motivo degli altri workflow: il cron nativo di Vercel (Hobby) non è
# affidabile, GitHub Actions chiama l'endpoint dall'esterno indipendentemente.

on:
  schedule:
    - cron: '0 12 * * *'
  workflow_dispatch: {}

jobs:
  trigger-market-rumors:
    runs-on: ubuntu-latest
    steps:
      - name: Chiama l'endpoint di ingestione Market Rumors
        run: |
          response=$(curl -s -w "\n%{http_code}" -X POST https://ls-job-intelligence.vercel.app/api/market-rumors)
          http_code=$(echo "$response" | tail -n1)
          body=$(echo "$response" | sed '$d')
          echo "HTTP status: $http_code"
          echo "$body"
          if [ "$http_code" != "200" ]; then
            echo "::error::Market-rumors endpoint returned HTTP $http_code"
            exit 1
          fi
